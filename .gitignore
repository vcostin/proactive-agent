node_modules/
dist/
.DS_Store

# Tauri
src-tauri/target/
src-tauri/gen/

# Sidecar binaries are populated by build script, not committed
binaries/*
!binaries/.gitkeep

# Local DB data and downloaded models (populated at runtime, never committed)
data/
models/

# Machine-local cargo config (contains hardcoded protoc path)
src-tauri/.cargo/config.toml

# Claude local settings
.claude/
parakeet-tdt-0.6b-v3-fastapi-openai/

# GitNexus — generated context files, regenerated by `gitnexus analyze`
# Not committed: meta.json.lastCommit would always lag behind HEAD otherwise
CLAUDE.md
AGENTS.md
