# Dedup and cancel from Debug → Scheduler

Type: grilling
Status: open
Blocked by:

## Question

What counts as a duplicate deferred follow-up (message text, trigger, both, something else), what happens on collision (skip new, replace pending, allow stack), and what cancel surface does Debug → Scheduler need beyond today's fire-now / test-defer?
