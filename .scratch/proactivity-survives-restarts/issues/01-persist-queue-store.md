# Persist deferred queue store

Type: grilling
Status: claimed
Blocked by:

## Question

Where and in what format should the pending deferred queue live on disk so it reloads across app restarts — JSON next to config, LanceDB, or something else — and what is the minimal schema we commit to for this P0?
