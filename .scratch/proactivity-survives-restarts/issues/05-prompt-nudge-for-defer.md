# Prompt nudge for deferral

Type: grilling
Status: open
Blocked by:

## Question

Given the default `persona_prompt` already instructs the model to emit `<defer>` sparingly for useful follow-ups, what (if any) change is required for P0 — leave as-is, light wording tweak, or examples/timing guidance — without inventing a new deferral system?
